#!/system/bin/sh

# Property will revert to default on next reboot
# Nothing special needed for uninstall
echo "EnableTheRemoveGameFPSLimitDevSettingAutomatically: module uninstalled, property will reset on reboot" >> /dev/kmsg
